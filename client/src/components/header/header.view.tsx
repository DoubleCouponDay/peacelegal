import React from "react"
import {Navigator} from '../navigator/navigator.presenter'

export default function headerview(tabtitle:string): JSX.Element {
    return (        
        <Navigator />     
    )
}